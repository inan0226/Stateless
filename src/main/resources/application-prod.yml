spring:
  datasource:
    url: jdbc:mysql://[추후생성할_MySQL주소]:3306/팀원DB명?serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: [MySQL계정명]
    password: [MySQL비밀번호]
  jpa:
    hibernate:
      ddl-auto: update # 엔티티 변경 사항만 DB에 반영 (운영에서 create-drop은 위험합니다)