spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop # 실행 시 테이블 생성, 종료 시 삭제 (메모리 DB에 적합)
    show-sql: true
    properties:
      hibernate:
        format_sql: true